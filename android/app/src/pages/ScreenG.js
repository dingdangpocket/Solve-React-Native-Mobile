import React,{useState} from 'react';
import {
    View,
    Text,
    Button,
    StyleSheet
  } from 'react-native';


const ScreenG = ({ navigation,route}) => {
    // const {name,id}=route.params   
    return (
        
        <View>
            <Text>G页面</Text>
        </View>
    );
}

const styles = StyleSheet.create({})
export default ScreenG;